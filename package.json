{
  "name": "kwikloom",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for kwikloom frontend and backend",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix kwikloom-frontend\" \"npm start --prefix kwikloom-backend\"",
    "frontend": "npm run dev --prefix kwikloom-frontend",
    "backend": "npm start --prefix kwikloom-backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "lucide-react": "^0.525.0"
  }
}
